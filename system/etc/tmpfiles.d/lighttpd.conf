# /etc/tmpfiles.d/lighttpd.conf
# Ensure /var/log/lighttpd exists with correct ownership on boot
# Prevents lighttpd crash on Armbian (which mounts tmpfs/zram on /var/log)
d /var/log/lighttpd 0750 www-data www-data -
